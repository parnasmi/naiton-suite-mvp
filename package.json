{
  "name": "naiton-monorepo-mvp-v1",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=22.15.0",
    "pnpm": ">=9.15.1"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.1",
    "typescript": "^5.8.3"
  }
}
